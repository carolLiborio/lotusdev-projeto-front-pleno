import React from 'react';
import {Typography,makeStyles,Button} from '@material-ui/core'

const useStyle = makeStyles((theme)=>({

}));

const SearchFav = () => {
    const classes = useStyle();
    
    return(
        <Typography>Chama</Typography>
    );

}
